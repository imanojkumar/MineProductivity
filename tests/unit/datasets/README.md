# Unit Tests — mineproductivity.datasets

## Purpose

Unit tests for `src/mineproductivity/datasets/`, mirroring its structure one-to-one.

## Scope

Isolated tests for `mineproductivity.datasets` only. Cross-package behavior belongs in `tests/integration/`.

## Responsibilities

- Cover every public symbol exported by `mineproductivity.datasets` once implemented.

## Contents

- Placeholder — no implementation exists yet in the corresponding source package.

## Dependencies

`pytest`.

## Future Work

Add tests as `mineproductivity.datasets` is implemented, in the same pull request.

## References

- Reference Implementation Blueprint v1.0
