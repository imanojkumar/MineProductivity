# Unit Tests — mineproductivity.connectors

## Purpose

Unit tests for `src/mineproductivity/connectors/`, mirroring its structure one-to-one.

## Scope

Isolated tests for `mineproductivity.connectors` only. Cross-package behavior belongs in `tests/integration/`.

## Responsibilities

- Cover every public symbol exported by `mineproductivity.connectors` once implemented.

## Contents

- Placeholder — no implementation exists yet in the corresponding source package.

## Dependencies

`pytest`.

## Future Work

Add tests as `mineproductivity.connectors` is implemented, in the same pull request.

## References

- Reference Implementation Blueprint v1.0
