# API Reference Documentation

## Purpose

Home for generated API reference documentation covering the `mineproductivity` package.

## Scope

Auto-generated or semi-automated API reference (e.g., via mkdocstrings). Does not include hand-written conceptual or how-to documentation.

## Responsibilities

- Provide a build target for automated API reference generation, once wired up.

## Contents

- Placeholder — automated generation is not wired up yet. Until then, each implemented package's public API is documented directly in its own `README.md` under `src/mineproductivity/<package>/README.md` (`core`, `events`, `ontology`, `registry`, `plugins`, `connectors`, `kpis`) and enforced by that package's `test_public_api.py`.

## Dependencies

Will depend on `mkdocs` and an API-doc generator (e.g., `mkdocstrings`) once wired up.

## Future Work

Wire up automated API reference generation as part of the documentation CI workflow.

## References

- Reference Implementation Blueprint v1.0
